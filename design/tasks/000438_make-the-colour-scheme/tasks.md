# Implementation Tasks

- [~] Update `:root` custom properties in `app/globals.css` with the new palette values (all 10 tokens: `--brand-green`, `--brand-green-light`, `--brand-green-dark`, `--accent-amber`, `--accent-teal`, `--warm-cream`, `--warm-sand`, `--text-primary`, `--text-secondary`, `--border-light`)
- [~] Verify the `@theme inline` block still correctly maps all updated CSS variables to Tailwind colour utilities
- [ ] Visually check the homepage (header, search bar, welcome cards, footer) against the new palette
- [ ] Visually check the results view (bird cards, location cards, "show more" buttons, loading skeletons) for contrast and cohesion
- [ ] Confirm WCAG AA contrast ratios for body text, secondary text, and white-on-header combinations
- [ ] Verify semantic status badges (conservation, location type) and error states still look correct on the new backgrounds